import 'package:flutter/material.dart';

void main() => runApp(MaterialApp(
    home: Scaffold(
        appBar: AppBar(
          title: Text("Demo: Home Page"),
          backgroundColor: Colors.black87,
        ),
        body: Image.asset('image/luffy.png'))));
